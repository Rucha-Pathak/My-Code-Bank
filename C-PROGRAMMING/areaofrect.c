void main()
{
	
	 int len,bre;
	 printf("Enter length and breadth: ");
	 scanf("%d%d",&len,&bre);
     int area = len*bre;
	 printf("Area of rectangle: %d",area);
	 printf("\n\nPerimeter of rectanle : %d", 2*(len+bre))	;
	  
}